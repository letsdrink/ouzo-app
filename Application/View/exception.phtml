<?php

use Ouzo\Utilities\Path;

$httpCode = $exceptionData->getHttpCode();
if ($httpCode == 404) {
    require(Path::join(__DIR__, '404.phtml'));
} else {
    require(Path::join(__DIR__, '_error.phtml'));
}
